/submit/expense/
    POST, returns a json
    input : date (optional), text, amount, user
    output: status:ok

/submit/income
    POST, returna a json
    input: date(optional), text, amount, user
    output: status:ok